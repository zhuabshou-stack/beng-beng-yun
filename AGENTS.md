# 《蹦蹦云》项目协作规则

- 本文件适用于当前项目；同时遵守上级 `D:\cesi2\AGENTS.md`。
- 使用中文沟通与记录；代码标识符使用清晰英文。
- 历史版本和 `版本备份` 只读，禁止覆盖、删除或回写。
- 修改前先扫描现状；保留已验证功能，不擅自修改核心物理、碰撞、经济或主循环。
- 场景必须由 Cocos Creator 正式创建和保存，不手写 scene/meta，不伪造 UUID。
- 不使用 `window`、`document`、`navigator`；平台能力经 `DouyinBridge` 隔离。
- 动态逻辑使用 deltaTime；复用对象池和 SpriteFrame 高清替换接口。
- 代码或玩法修改必须通过 TypeScript 检查、Creator 导入检查和实际预览回归。
- 每完成一个版本或重要修改，必须同步更新 `docs/HANDOFF.md` 与 `docs/ROADMAP.md`。
- 上下文压缩、关键决定不确定、会话过长、开始大版本前或交接可能不完整时：停止代码修改，先更新交接文件，并告知用户“交接文件已更新，请新建 Codex 聊天并继续。”
- 交接不完整时不得凭记忆继续开发。
- 用户授权：仅可向指定的《蹦蹦云》私人 GitHub 仓库同步经过测试和敏感信息检查的正式版本；仓库必须保持 Private，禁止上传构建缓存、版本备份和平台凭证。
