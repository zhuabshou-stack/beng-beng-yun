# 《蹦蹦云》当前交接

更新时间：2026-07-19
当前版本：v2.9.3 HTML 高一致度复刻修正版
当前分支：`agent/html-parity`
分支起点：`ee11e1c12532754493f5ee62016f7d6bcd18590e`
本轮起点：`e53c429a5b21237e71d427f18d6434bee681ade4`
项目：`D:\cesi2\蹦蹦云-Cocos工程\cese`
只读备份：`D:\cesi2\版本备份\游戏1-蹦蹦云\v2.9.3`

## 分支与保护边界

- `master` 保持不动；`agent/v2.8.0` 仅作备份。
- HTML 复刻只在 `agent/html-parity`；不合并 master，等待用户试玩。
- `D:\cesi2\游戏本体`、所有 `legacy` 和历史版本备份只读。
- 不上传抖音平台，不发布，不提交审核。

## 正式场景与构建

- 流程：`Boot.scene → Home.scene → Game.scene → Home.scene`。
- 构建只包含 Boot、Home、Game，Boot 为初始场景。
- `Main.scene`、`HomeShell.scene` 保留但不参与正式构建。
- 方向：Portrait。
- 浏览器目录：`D:\cesi2\蹦蹦云-Cocos工程\cese\build\web-mobile-html-parity`。
- 抖音开发者工具导入：`D:\cesi2\蹦蹦云-Cocos工程\cese\build\bytedance-mini-game`。
- `build/`、`profiles/`、`library/`、`temp/`、`local/` 不提交 Git。

## v2.9.3 已完成

- 云生成与移动边界同时扣除真实半宽、20 单位边距和移动振幅，云体不再被左右裁切。
- 玩家删除循环穿越，以 60 单位视觉半宽限制在 `±480`，到边界清除朝外速度。
- GameManager 改为 0.1 秒大帧上限、1/60 秒物理子步；CameraRig 使用同一上限，解决低帧下降与镜头时基错位。
- Camera/Canvas/背景/全屏面板动态使用实际可见尺寸；安全区只影响 UI。
- 设置页深色主题覆盖背景、遮罩、面板、文字和按钮；深色模式保持旧版“始终开启”。
- 启动页、主页、开场动画、HUD、暂停、技能栏按旧 HTML 结构和节奏重整。
- P/Escape 暂停；暂停页补齐重新开始；手机技能触控面积与桌面 1～6 键均保留。

## 继续保留的完整迁移

- 页面：主页、游戏、暂停、结算、关卡完成、皮肤、技能、排行、统计、设置、教学。
- 玩法：旧版跳跃/水平移动/镜头/四种云/碰撞/Combo/分数/高度/关卡目标。
- 内容：金币、星星、羽毛、六套皮肤、六种技能。
- 反馈：落云、弹簧、Combo、收集、里程碑的粒子、浮字、拖尾和震屏。
- 数据：最高分、金币、本地前十、皮肤、技能、设置及旧存档键兼容。
- 底层：SceneNavigator、StorageService、DouyinBridge、AudioManager 和固定对象池。

## 关键参数

| 项目 | v2.9.3 |
|---|---:|
| 参考帧率 / 世界比例 | 60 FPS / ×2 |
| 重力 / 起跳速度 | 2160 / 1680 |
| 水平速度 / 加速度 | 720 / 2160 |
| 松手阻尼 | `0.92^(dt×60)` |
| 玩家碰撞半径 / 视觉半宽 | 36 / 60 |
| 云尺寸 / 间距 | 140×40 / 100～140 |
| 屏幕边距 | 20 |
| 物理步长 / 大帧上限 | 1/60 / 0.1 秒 |
| 弹簧倍率 | 2.0 |
| 镜头线 / 跟随率 | 0.40 / 2.45 |
| 四种云概率 | 75% / 5% / 10% / 10% |
| 星星 / 金币 / 羽毛 | 25% / 35% / 8% |
| 对象池 | 拖尾 20、粒子 128、浮字 12 |

## 验证证据

- Creator 3.8.8 web-mobile 和 bytedance-mini-game 最终构建成功，项目错误 0。
- Creator 内置 TypeScript、`git diff --check`、禁用平台全局检查通过。
- 浏览器交互覆盖 Boot → Home → Game → Home、首次教学、开场、暂停、技能、设置和自然结算。
- 玩家两侧边界：`-480 / +480`，朝外速度为 0。
- 25 个活动云与移动云模拟 10 秒：安全边缘 520，最外边缘 491.74，无越界。
- 下降 playing 段 46 帧重复 playerY 为 0；80ms 大帧完整推进 80ms。
- 10 秒节点/堆：368/66.2MB → 354/55.9MB，无持续增长。
- 抖音包存在 `game.js`、`game.json`、`project.config.json`。
- 控制台项目异常 0；本地服务器仅有非项目 `favicon.ico` 404。

## 尚未迁移 / 未验证

- 旧 HTML 无独立正式音频文件；只保留 AudioManager 替换接口。
- 正式抖音排行、广告、云存档、支付、商业化不在旧版闭环内，未新增。
- 抖音开发者工具最终画面和真机安全区、触控、性能仍需用户侧验证。
- 全部手感与视觉项目：**等待用户试玩确认。**

## 下一步

停止继续开发，不合并 master。用户试玩后只根据明确反馈在 `agent/html-parity` 小范围修正；未获得确认不开始下一版本。

交接文件已更新，请新建 Codex 聊天并继续。
