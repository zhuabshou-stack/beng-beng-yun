# 《蹦蹦云》当前交接

更新时间：2026-07-23
当前版本：v2.9.4 HTML 高一致度复刻交互修正版
当前分支：`agent/html-parity`
分支起点：`ee11e1c12532754493f5ee62016f7d6bcd18590e`
本轮起点：`cde64928eeee108022b51fbafd6cd0f372f50e3f`
项目：`D:\cesi2\蹦蹦云-Cocos工程\cese`
只读备份：`D:\cesi2\版本备份\游戏1-蹦蹦云\v2.9.4`

## 分支与保护边界

- `master` 保持不动，`agent/v2.8.0` 只作备份。
- HTML 复刻只在 `agent/html-parity`；不合并 `master`，等待用户试玩。
- `D:\cesi2\游戏本体`、所有 `legacy` 和历史版本备份只读。
- 不上传抖音平台，不发布，不提交审核。

## 正式场景与构建

- 流程：`Boot.scene → Home.scene → Game.scene → Home.scene`。
- 构建只包含 Boot、Home、Game，Boot 为初始场景。
- `Main.scene`、`HomeShell.scene` 保留但不参与正式构建。
- 方向：Portrait。
- 浏览器目录：`D:\cesi2\蹦蹦云-Cocos工程\cese\build\web-mobile-html-parity`
- 抖音开发者工具导入：`D:\cesi2\蹦蹦云-Cocos工程\cese\build\bytedance-mini-game`
- `build/`、`profiles/`、`library/`、`temp/`、`local/` 不提交 Git。

## v2.9.4 已完成

- 主页和游戏内所有全屏面板使用输入阻断并停止触摸传播，关闭动作延迟到当前触摸结束后处理。
- 主页设置不再回弹到“我的统计”；深色模式只切换主题并保持设置面板打开。
- 游戏设置打开时暂停；关闭只恢复游戏，不触发暂停、主页或技能。
- 技能按钮对阶段、暂停、装备、次数、冷却和效果状态给出明确原因；手机点击和键盘 `1～6` 共用真实技能逻辑。
- 六种技能沿用旧 HTML 的消耗、冷却、持续时间和效果。
- 开始按钮立即禁用；Game 预加载和 0.78 秒 Cocos Tween 过渡并行，完成后进入 0.38 秒 Game 淡入。
- 不同云连续落点按 Combo 提高有上限的节奏、镜头和反馈；中断、死亡和重开恢复基础值。

## 关键参数

| 项目 | v2.9.4 |
|---|---:|
| 基础重力 / 跳跃速度 | 2160 / 1680 |
| 水平速度 / 加速度 | 720 / 2160 |
| 松手阻尼 | `0.92^(dt×60)` |
| 连跳节奏步进 / 上限 | 0.035 / 1.28 |
| 节奏重力缩放 | `tempo²` |
| 漏云重置距离 | 180 |
| 镜头跟随倍率上限 | 1.36 |
| 反馈倍率上限 | 1.80 |
| 起跳拉伸上限 | 1.24 |
| 主页过渡 / 游戏淡入 | 0.78 秒 / 0.38 秒 |

旧 HTML 的跳跃、重力、水平移动、云尺寸和概率等基础参数未改。旧版没有独立连跳速度变量；v2.9.4 的有界提速为用户本轮明确要求。

## 验证证据

- Creator 3.8.8 浏览器和抖音小游戏构建成功，项目错误 0。
- Creator 内置 TypeScript、`git diff --check` 和禁用平台全局检查通过。
- Chrome 390×844 完成 Home 设置/主题/关闭、开始过渡、Game、暂停、游戏设置关闭和返回主页流程，控制台项目错误 0。
- 开始按钮重复点击只进入一次；两个设置关闭动作均未命中下层按钮。
- 实际 `GameManager` 隔离运行逐项验证六种技能均触发真实状态和次数消耗。
- 抖音包根目录存在 `game.js`、`game.json`、`project.config.json`。

## 尚未验证

- 正式音频素材仍未提供，AudioManager 保留替换接口。
- 抖音开发者工具最终画面和真机安全区、触控、性能仍需用户侧验证。
- 所有视觉和手感项目：**等待用户试玩确认。**

## 下一步

停止继续开发，不合并 `master`。用户试玩后，只根据明确反馈在 `agent/html-parity` 小范围修正；未获得确认不开始下一版本。

交接文件已更新，请新建 Codex 聊天并继续。
