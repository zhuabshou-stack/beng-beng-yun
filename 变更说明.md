# 《蹦蹦云》v2.9.4 变更说明

## 版本定位

本版本是 `agent/html-parity` 分支上的 HTML 高一致度复刻交互修正版，起点提交为 `cde64928eeee108022b51fbafd6cd0f372f50e3f`。旧 HTML 版仍是 UI、流程、技能规则和基础物理的唯一参考；不修改 `master`，不合并主分支，不上传、发布或提交抖音审核。

## 本轮修复

- 主页设置面板使用全屏输入阻断，触摸开始、移动、结束和取消均停止传播。
- 面板关闭延迟到当前触摸结束后的下一调度周期销毁，避免同一次触摸继续命中下层“我的统计”等按钮。
- 深色模式只切换并持久化主题，不关闭或重建设置面板，也不重复注册监听。
- 游戏设置打开时自动暂停并拦截下层输入；“关闭”只关闭设置并恢复游戏。
- 技能不可用时显示明确原因：游戏阶段、暂停、未装备、次数耗尽、冷却中、效果已激活或缺少时光回溯历史。
- 六种技能继续使用旧 HTML 的真实消耗、持续时间和冷却，手机按钮和键盘 `1～6` 共用同一套逻辑。
- 开始按钮点击后立即禁用；Game 场景预加载与 0.78 秒过渡动画并行，两个条件都完成后才进入游戏。
- 过渡为主页轻微缩小淡出、两侧云雾掠过和提示淡出；Game 场景使用 0.38 秒淡入，不再出现长白屏或僵硬倒计时。
- 连续落到不同云时，以 Combo 为依据逐级提高跳跃节奏、镜头追随和反馈强度；重复落同一云、明显漏云、死亡或重开会恢复基础节奏。

## 参数变化

旧 HTML 使用固定跳跃速度和重力。本轮保留其基础换算值，并按用户明确要求增加有上限的连续落云节奏增强；该增强不是旧 HTML 原有独立参数。

| 参数 | v2.9.3 | v2.9.4 | 说明 |
|---|---:|---:|---|
| 基础跳跃速度 | 1680 | 1680 | 首跳不变 |
| 基础重力 | 2160 | 2160 | 基础值不变 |
| 连跳节奏步进 | 无 | 每级 Combo +0.035 | 仅不同云成功落点累计 |
| 连跳节奏上限 | 1.0 | 1.28 | 防止无限加速 |
| 节奏重力缩放 | 1.0 | `tempo²` | 加快周期但尽量保持跳高 |
| 漏云重置距离 | 无 | 180 | 明显错过平台后重置 |
| 镜头跟随倍率 | 1.0 | 最高 1.36 | 随 Combo 有界增强 |
| 粒子/拖尾反馈倍率 | 1.0 | 最高 1.80 | 不依赖强震屏制造速度感 |
| 起跳拉伸 | 1.10 | 最高 1.24 | 随 Combo 有界增强 |
| 主页过渡 | 直接切场景 | 0.78 秒 | 与场景预加载并行 |
| 游戏淡入 | 1.66 秒白闪序列 | 0.38 秒 | 移除长白屏 |

## 六种技能对照

| 技能 | 金币消耗 | 冷却 | 持续/效果 |
|---|---:|---:|---|
| 护盾 | 80 | 15 秒 | 抵挡一次死亡 |
| 磁铁 | 60 | 20 秒 | 3 秒，吸附半径 150 旧版像素 |
| 慢动作 | 100 | 25 秒 | 5 秒，重力减半 |
| 幽灵 | 120 | 30 秒 | 4 秒，穿透云体限制 |
| 二段跳 | 50 | 10 秒 | 当前空中追加一次 70% 跳跃 |
| 时光倒流 | 解锁等级 3 | 45 秒 | 回到约 3 秒前的状态 |

## 验证结果

- Creator 3.8.8 浏览器构建于 2026-07-23 23:50:28 成功，抖音小游戏构建于 23:52:17 成功，Creator 项目错误计数为 0。
- Creator 内置 TypeScript 编译、差异格式检查和禁用浏览器全局检查通过。
- Chrome 390×844 竖屏实际交互覆盖：主页设置、深色切换、设置关闭、开始按钮重复点击、过渡、教学、游戏、暂停、游戏设置关闭、返回主页。
- 主页设置关闭未弹到统计；游戏设置关闭未弹到暂停或下层按钮；浏览器项目错误和警告为 0。
- 使用实际 `GameManager` 代码进行隔离运行检查，六种技能均成功触发对应游戏状态并正确扣减一次使用次数。
- 抖音构建根目录存在 `game.js`、`game.json`、`project.config.json`。
- 未进行 600 秒自动游玩；自动化结果不用于证明游戏是否好玩。

## 待用户试玩确认

- 连续落不同云后的提速幅度、上限和重置时机。
- Combo 对拖尾、拉伸、粒子、浮字和镜头追赶的增强节奏。
- 开始过渡的速度、云雾遮挡比例和 Game 淡入节奏。
- 手机端设置遮挡、技能触控面积和六种技能的实战可读性。
- 所有视觉和手感结论：**等待用户试玩确认。**

## 未做事项

- 未修改无关 UI、碰撞体、云概率或基础收集逻辑。
- 未修改历史备份、`Main.scene` 或 `HomeShell.scene`。
- 未合并 `master`，未上传抖音平台，未发布或提交审核。
