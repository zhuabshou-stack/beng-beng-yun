# 蹦蹦云 v2.9.2 变更说明

## 版本定位

从 v2.8.0 提交 `ee11e1c12532754493f5ee62016f7d6bcd18590e` 建立 `agent/html-parity`。旧 HTML 是本分支唯一标准；v2.8.0 UI、精准落点和 Combo 8/12 冲刺不再作为正式实现保留。

没有修改 `master` 或 `agent/v2.8.0`，没有合并主分支，没有上传抖音平台，也没有发布或提交审核。

## 完成内容

- 按旧版重做主页结构和视觉层次：金币、设置、Logo、开始按钮、六个功能卡片、背景粒子与环境云。
- 恢复皮肤、技能商店、排行榜、旧版四项统计、旧版三项设置、暂停菜单、结算、关卡完成和首次教学。
- 迁移六套皮肤及解锁价格；迁移护盾、磁铁、时间减速、幽灵、二段跳、时光倒流六种技能。
- 迁移金币、星星、羽毛；羽毛按旧版 30 帧滑翔效果处理。
- 将跳跃、重力、水平控制、云尺寸/间距、四种云概率、弹簧倍率、Combo 和镜头换算为 Cocos 秒单位。
- 删除 v2.8.0 的精准落点奖励、不同云 Combo 超时规则、Combo 8/12 冲刺和弹簧专用镜头节奏，避免并存两套逻辑。
- 恢复旧版 Combo：每次成功落云递增，下降错过云时清零；每 3 连击提示、每 5 连击震屏并增加 `floor(combo/5)` 奖励。
- 恢复旧版落云、弹簧、收集、里程碑粒子/浮字/拖尾；拖尾、粒子、浮字固定池化。
- 保留 Boot → Home → Game → Home、SceneNavigator、StorageService、DouyinBridge、AudioManager 和平台构建结构。

## 参数对照

| 项目 | 旧 HTML | v2.8.0 | v2.9.2 HTML 复刻 |
|---|---:|---:|---:|
| 重力 | 0.30 px/帧² | 1500/秒² | 2160/秒² |
| 起跳速度 | 14 px/帧 | 920/秒 | 1680/秒 |
| 水平速度 | 6 px/帧 | 380/秒 | 720/秒 |
| 水平加速度 | 0.30 px/帧² | 2600/秒² | 2160/秒² |
| 阻尼 | 0.92/帧 | 0.84/帧 | 0.92/帧等效 |
| 玩家半径 | 18 px | 36 | 36 |
| 云尺寸 | 70×20 px | 144×42 | 140×40 |
| 云间距 | 50～70 px | 130～170 | 100～140 |
| 弹簧倍率 | 2.0 | 1.62 | 2.0 |
| 镜头阈值 | 40% | 41% | 40% |
| 镜头追随 | 差值×0.04/帧 | 3.15/秒且限速 | 2.45/秒等效，不限旧版外的追赶上限 |
| Combo | 落云累加、错过清零 | 异云+超时+8/12冲刺 | 落云累加、错过清零，无冲刺 |

换算基准为 60 FPS、Cocos 世界比例 2；源码集中在 `assets/scripts/core/GameConfig.ts`。

## 验证结果

- Creator 3.8.8 正式导入 `LegacyProgression.ts`、`LegacyGamePanels.ts` 并生成 meta。
- Creator 内置 TypeScript 编译器：0 错误；`git diff --check`：0 错误；项目脚本无 `window`、`document`、`navigator`。
- 浏览器构建 `web-mobile-html-parity` 成功，只包含 Boot、Home、Game，Boot 为初始场景。
- 720×1280 浏览器真实点击覆盖：首页、皮肤、技能、排行空态/有数据、统计、设置、首次教学、游戏、暂停、结算、返回主页和刷新后排行保存。
- 最终生成抽样：普通 20、脆弱 3、弹簧 2、移动 3；三类收集物池预热 28 个，随机生成时扩至 29 个后稳定。
- Game 节点在最终 20 秒抽样中由 289 回落并稳定为 288；固定特效池为拖尾 20、粒子 128、浮字 12，未见持续增长。
- 浏览器应用异常 0；唯一 404 是测试服务器自动请求的 `favicon.ico`，不是项目资源。
- 抖音构建成功；`game.js`、`game.json`、`project.config.json` 均存在，方向为 Portrait。

## 截图

- `docs/screenshots/html-parity/home.png`
- `docs/screenshots/html-parity/game.png`
- `docs/screenshots/html-parity/pause.png`
- `docs/screenshots/html-parity/result.png`
- `docs/screenshots/html-parity/level-complete.png`
- `docs/screenshots/html-parity/skins.png`
- `docs/screenshots/html-parity/skills.png`
- `docs/screenshots/html-parity/ranking.png`
- `docs/screenshots/html-parity/statistics.png`
- `docs/screenshots/html-parity/settings.png`

## 尚未迁移与待确认

- 旧 HTML 没有独立音频素材；当前保留 AudioManager 替换接口，未提交正式声音文件。
- 正式抖音排行榜、广告、云存档、支付和商业化不是旧版闭环，本分支不新增。
- 真实抖音开发者工具画面与真实手机触控/性能需用户侧继续验证。
- 跳跃、水平控制、镜头、弹簧、玩家/云比例、UI 和动画节奏：**等待用户试玩确认。**
