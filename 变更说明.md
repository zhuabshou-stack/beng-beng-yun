# 《蹦蹦云》v2.11.0 变更说明

## 版本定位

本版本是 `agent/v2.11.0-ui-polish` 分支（自 v2.10.0 切出）的界面优化版。依据用户定调：**HTML 原版的页面设计、进场、手感、技能是视觉与体验标杆**，当年转 Cocos 只因 HTML 画质在手机上不行。因此本轮新增 UiKit 设计系统，把全部界面视觉 1:1 对齐 HTML 原版实测规格。玩法、物理、存档、平台桥接零改动。

## 本轮新增

### 1. UiKit 设计系统（assets/scripts/ui/UiKit.ts）

设计令牌全部取自 HTML css/styles.css 实测值：面板渐变 #1a1a2e→#16213e、主渐变 #667eea→#764ba2、开始按钮三段渐变 #f093fb→#f5576c→#ff6b6b、金色 #ffd700、连击红 #ff6b6b、卡片高亮条六色渐变、技能六色渐变。提供：垂直/水平渐变带绘制、圆角内渐变（逐层 roundRect 叠色）、多层投影模拟、Label 粗体+文字阴影（引擎 isBold/enableShadow，此前工程从未使用）、统一按压反馈（保存基准缩放，修复旧实现还原 (1,1,1) 破坏预缩放节点的隐患）、Toggle 开关控件。

### 2. 主页

- 开始按钮：单色平涂 → 三段渐变 + 外发光投影 + 常驻呼吸光晕（HTML playBtnPulse）+ 按下 0.97。
- 功能卡：320×150 → 满宽 500×230（对齐 HTML 内容区占比），渐变高亮条按 HTML 六色，图标/字号放大。
- 标题粗体+文字阴影；金币徽章金边金字胶囊；设置齿轮正圆。
- 入场动画：logo 落下淡入 + 六卡依次浮现（HTML floatUp/cardIn 等价物）。
- 背景渐变 48 段 → 128 段平滑。

### 3. 游戏 HUD

分数/最高分/金币/连击加粗 + 文字阴影；连击基色 #ff6b6b + 红晕；金币 #ffd700；进度条轨道白 30% + 填充 #f093fb→#ffd166 渐变 + 金色端点；顶部按钮 66→100、技能栏 76→110、统计条加宽——整体占比对齐 HTML。

### 4. 弹窗全家桶

- 面板壳统一：HTML 渐变底 + 圆角 48 + 白 10% 边 + 大投影；遮罩主页面板黑 50%/游戏内 60%；标题粗体。
- 设置（主页+游戏内）：文字开关 → HTML 规格真 Toggle（轨道+滑块，开=主渐变）。
- 技能商店：技能专属渐变图标底（六色）+ 横卡布局 + ScrollView 滚动（修复面板高 1500 小屏溢出）。
- 排行榜：金色标题、前三名整行金/银/铜底色。
- 皮肤：选中 #ffd700 金边 + 金色透明底，未解锁半透明。
- 结算面板（HTML §4.2）：大号分数（新纪录变金色 + "🎉 新纪录"标记）、四行统计、三按钮——新增"分享成绩"（走 PlatformService.share，HTML 原版即有分享）。
- 暂停面板：主次按钮分级（继续=主渐变胶囊、次级=白 10% 菜单钮、主页=粉渐变）。
- 教学/关卡完成面板同步换新壳与渐变按钮。

### 5. 清理

- 删除死代码 `visual/DreamyHome.ts`（278 行，全工程零引用）。
- Boot 进度条：填充段直角 rect → 圆角渐变（修复 0%/100% 四角穿出）。
- `build-templates/web-mobile/favicon.ico`：经 Cocos 正规模板机制固化，消除每次构建后的 favicon 404。

## 版本号

- `GameConfig.version`：2.10.0 → 2.11.0；Boot 标签、README、本文档同步。

## 未修改项

- 玩法、物理、碰撞、经济、技能规则、关卡与全部存档键语义零改动。
- 平台桥接（PlatformService/DouyinBridge/WechatBridge）、音频内容、场景文件未动。

## 验证结果（2026-09-14）

- 构建：Creator 3.8.8 命令行 web-mobile 构建成功；TypeScript 业务代码 0 报错。
- 真实 Chrome 自动化回归（tools/browser-regression/regression.js）：**10/10 全通过**——启动→主页（v2.11.0 标签）→真实点击开始→真实触摸左右移动（210→132→295）→键盘暂停/恢复→返回主页→分享入口→设置 Toggle 持久化（存档 0/1 切换实测）→音频加载与 BGM 播放→控制台零错误、零 404。
- 截图对照：主页与 HTML 原版并排比对，开始按钮/卡片/高亮条观感一致；暂停/设置面板达到 HTML 规格。
- 玩法回归：跳跃、碰撞、连击、金币、教学、暂停、结算全流程无异常。

## 已知限制

- 中文粗体依赖系统字体渲染，字重提升幅度有限（不引入字体文件的前提下无法更强）。
- emoji 图标在抖音/微信真机的渲染字形以真机验收为准（HTML 原版同样使用系统 emoji）。
- 无头浏览器后台节流可能影响长时自动化测试的移动读数（测试框架限制，非游戏缺陷）。

## 待用户试玩确认

- 新版界面的观感与操作是否达标；开始按钮光晕/入场动画节奏是否合适；音效与 BGM（延续上轮待确认项）。
