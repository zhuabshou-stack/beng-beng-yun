# 《蹦蹦云》v2.10.0 变更说明

## 版本定位

本版本是 `agent/v2.10.0-multiplatform` 分支（自 `agent/html-parity` 的 v2.9.4 切出）的多平台上架适配版。目标：把现有游戏上架到抖音小游戏与微信小游戏；快手小游戏因官方仅支持企业主体入驻，本轮暂缓（前置条件清单已备）。不修改玩法、物理参数与存档语义；不动 master 与历史备份。

## 本轮新增

### 1. 多平台桥接层

- 新增 `assets/scripts/platform/PlatformService.ts`：业务代码唯一的平台入口，提供分享、震动、前后台监听、安全区换算与平台名。运行时按 `ks`（快手预留）→ `wx`（微信）→ `tt`（抖音）→ 浏览器 的顺序自动识别。
- 新增 `assets/scripts/platform/WechatBridge.ts`：与 DouyinBridge 完全同构的微信实现（shareAppMessage、vibrateShort、onShow/onHide/offHide、getSystemInfoSync 安全区换算，含相同的安全降级默认值）。
- `DouyinBridge` 保留为抖音专用实现，由 PlatformService 内部委托，不再被业务代码直接引用。
- 迁移 9 处调用点：`GameManager`（前后台自动暂停、落云震动）、`HomeController` 与 `ResultController`（分享）、`HomeSceneBootstrap`(主页分享)、`DreamyHUD`（安全区布局）。
- 主页分享状态文案改为按平台动态生成：浏览器显示"分享功能将在小游戏环境启用"，抖音/微信显示"已打开分享入口"。

### 2. 基础音频接入

- 新增 `tools/generate-audio.js`：纯 Node 程序化合成 8 个音效（ui/jump/spring/coin/star/combo/milestone/gameOver）与 1 段 9.6 秒可无缝循环的 BGM（C-Am-F-G 柔和垫 + 琶音）。全部自产正弦波 + 包络，无任何第三方素材，规避版权与平台合规风险。
- 新增 `assets/resources/audio/`（约 540KB，16bit 单声道 22050Hz wav）与 `assets/scripts/core/AudioCatalog.ts`：Boot 场景启动时从 resources 分包加载并注册进 AudioManager；单个资源失败仅降级为无声并告警，不阻塞流程。
- 主页 `onEnable` 尝试启动 BGM；注册 `TOUCH_START`/`KEY_DOWN` 兜底，规避浏览器自动播放限制。音频开关沿用既有设置项（cloudBounceSound / cloudBounceMusic）。

### 3. 平台构建与包体

- 抖音构建启用"引擎分离"插件（separateEngine）：主包 7.2MB → 2.4MB，满足抖音 4MB 主包限制；`game.json` 自动写入官方 cocos 3.8.8 引擎插件声明。
- 微信构建同样启用引擎插件：前提是**非调试模式**（官方文档明确），且平台选项需写入保存的构建配置 `profiles/v2/packages/wechatgame.json`（CLI key=value 不会合并平台选项）。最终微信包 2.2MB，`game.json` 写入官方 cocos 3.8.8 引擎插件声明。
- 微信构建 AppID 暂为微信官方测试号 `wx6ac3f5090a6b99c5`，正式上架前由用户注册小游戏账号后替换。

### 4. 平台上架文档

- 新增 `docs/微信上架准备清单.md`：账号注册、类目、软著/备案说明、隐私保护指引、开发者工具流程、审核前自查。
- 新增 `docs/快手前置条件清单.md`：企业主体、软著、自审自查报告等硬性条件与"微信包 + kwaiadapter 自动适配"技术路线备忘。

## 版本号

- `GameConfig.version`：2.9.4 → 2.10.0；Boot 场景版本标签、README、本文档同步。

## 未修改项

- 玩法、物理、碰撞、经济、技能、关卡与全部存档键语义（`cloudBounce*`）零改动。
- `StorageService` 基于 `sys.localStorage`，引擎层已跨平台，无需改动。
- 场景文件、历史备份、`Main.scene`、`HomeShell.scene` 未触碰。

## 验证结果（2026-09-14）

- 构建：Creator 3.8.8 命令行成功构建 web-mobile、bytedance-mini-game、wechatgame 三端。
- 静态检查：TypeScript 编译业务代码 0 报错（63 个报错全部位于引擎自带声明文件，属既有环境噪音）；`window/document/navigator` 全项目 0 命中。
- 真实浏览器回归（Playwright + 无头 Chrome 390×844，`tools/browser-regression/regression.js`）：**9/10 通过**。
  - 通过：启动→主页（版本标签 v2.10.0）→真实鼠标点击"开始游戏"进入对局→真实触摸按住左/右侧驱动玩家左右移动（540→60→605，健康帧率轮次）→键盘 P 暂停（面板出现、phase 切换）→再按 P 恢复→结算/游戏中返回主页→分享入口（状态栏显示平台动态文案）→设置音效开关（标签翻转 + `cloudBounceSound` 持久化为 0 并恢复）→9 个音频资源全部注册（BGM 处于播放状态、循环开启）→控制台 0 项目错误、0 资源 404。
  - 未稳定通过项：左右移动读数在无头浏览器持续运行数分钟后因 Chrome 后台节流（帧率坍塌、dt 被钳制）出现位移读数过小。功能本身在健康帧率轮次已完整通过；左移在全部 5 轮中均成功。
- 存档实测：最高分 80、金币、生涯统计（局数/跳跃/落云/星星/最高连击）、本地排行榜、技能解锁、教学标记均正确写入并在重载后保持。
- 首次教学流程实测：全新存档进入对局先显示教学面板，点击"明白了，出发"后正常开局（教学标记写入）。

## 已知问题与限制

- 浏览器端 HUD 顶部按钮使用默认安全区回退值，位置贴近屏幕上缘；真机（抖音/微信）会取真实 safeArea 正确下移。真机验收时确认。
- 无声游戏有被微信以"素材堆砌"驳回的风险 → 本轮已补基础音效，但正式素材（更丰富的 BGM/音效）仍待用户提供后替换 AudioManager 注册内容。
- 微信真机、抖音开发者工具与真机验收未做（需用户侧工具与账号）。

## 待用户试玩确认

- 音效与 BGM 的音量、音色与场景是否合适（当前为程序合成基础版）。
- 其余视觉与手感项延续 v2.9.4 待确认清单。
