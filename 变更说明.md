# 蹦蹦云 v2.9.3 变更说明

## 版本定位

本次是 `agent/html-parity` 上的 HTML 高一致度复刻修正版，起点为 v2.9.2 提交 `e53c429a5b21237e71d427f18d6434bee681ade4`。旧 HTML 仍是唯一标准；不修改 `master` 和 `agent/v2.8.0`，不合并主分支，不上传抖音平台。

## 已修复 Bug

- 云朵越界：生成位置计入真实云宽、屏幕边距和移动振幅；移动云每帧限制在完整可见范围。
- 玩家边界瞬移：删除左右循环穿越，按碰撞与视觉尺寸限制在屏幕内，到边界清除朝外速度。
- 下落卡顿：定位到 GameManager 将大帧截成 1/30 秒、而 CameraRig 使用原始 dt 的时基不一致。改为 0.1 秒接收上限与 1/60 秒子步，镜头使用同一帧上限。
- 全屏显示：Camera 正交高度、Canvas 根节点、背景、遮罩与过渡层按实际可见尺寸刷新；安全区不缩小游戏世界。
- 深色模式：按旧版固定深色主题统一背景、面板、文字、按钮和全屏遮罩，不保留无效主题切换。

## 已迁移与重整

- 启动页：旧版深色渐变、Logo 云、环境云、三色加载条、提示和短淡出。
- 主页：旧版金币/设置、Logo、开始按钮、六个功能入口、深色星空和环境云层次。
- 游戏开场：角色弹入、向上飞升、“准备开始/出发”、世界显现和闪白，不使用重新设计的倒计时。
- HUD：主页、暂停、设置、分数、最高分、金币、高度、星星、Combo、关卡进度和横向技能栏。
- 暂停：继续、重新开始、技能、排行、皮肤和返回主页；键盘 P/Escape 均可暂停/继续。
- 技能：手机触控面积保持约 49 CSS px，桌面恢复 1～6 键，显示冷却与可用状态。
- 结算、关卡完成、皮肤、技能、排行、统计、设置、教学和存档闭环继续沿用 v2.9.2 已迁移实现。

## 参数前后变化

旧 HTML 的跳跃、重力、水平移动、云尺寸/间距、云概率、碰撞半径、弹簧、Combo 和镜头参数未改。仅新增边界与低帧稳定参数：

| 参数 | v2.9.2 | v2.9.3 | 用途 |
|---|---:|---:|---|
| 玩家视觉半宽 | 未参与边界 | 60 | 防止角色视觉被裁切 |
| 屏幕边距 | 未统一 | 20 | 云朵完整可见 |
| 物理大帧处理 | `min(dt, 1/30)`，丢弃余量 | 最多 0.1 秒，按 1/60 子步完整推进 | 修复下降延迟 |
| 镜头大帧处理 | 使用原始 dt | 与物理同为最多 0.1 秒 | 统一时基 |
| 玩家左右边界 | 循环穿越 | 夹紧并清除朝外速度 | 用户明确要求覆盖旧版 wrap |
| 移动云范围 | 仅按中心/原点摆动 | 扣除云半宽和完整振幅 | 防止运动越界 |

## 下降问题定位记录

修改前的 5 秒记录中，下降段出现物理与相机使用不同 dt；当帧时间超过 33.3ms 时，玩家只推进 33.3ms，而相机推进完整帧，产生视线与角色节奏不一致。

修改后记录：

- 下降且仍为 playing 的 46 帧中，重复 playerY 为 0。
- 典型采样：`t=0.764s, playerY=379.82, velocityY=-12.14, cameraY=99.43`；`t=0.931s, 344.55, -373.94, 126.08`；`t=1.097s, 249.99, -732.07, 128.19`。
- 人工输入 `dt=0.08s` 时，GameManager 实际推进 `0.080000s`；此前最多仅推进 `0.033333s`。

这证明时基错误已修复，不代表主观手感已通过。下降节奏、镜头节奏与整体手感：**等待用户试玩确认。**

## 验证结果

- Creator 3.8.8：浏览器任务 19:10:49 构建成功，抖音任务 19:11:45 构建成功；项目错误 0。
- TypeScript、差异检查和禁用浏览器全局检查通过。
- 浏览器真实交互覆盖启动、主页、首次教学、开场、游戏、暂停、技能、设置、结算及返回主页。
- 1080 逻辑宽度下玩家夹在 `±480`，两侧朝外速度均归零。
- 25 个活动云及移动云模拟 10 秒无越界；安全边缘为 520，最外边缘 491.74。
- 10 秒节点/堆抽样从 368/66.2MB 到 354/55.9MB，无持续增长。
- 抖音包根目录存在 `game.js`、`game.json`、`project.config.json`，方向为 Portrait，构建场景仅 Boot/Home/Game。
- 未上传抖音平台，未发布，未提交审核。

## 截图

- `docs/screenshots/html-parity-v293/loading.png`
- `docs/screenshots/html-parity-v293/home.png`
- `docs/screenshots/html-parity-v293/entry.png`
- `docs/screenshots/html-parity-v293/game.png`
- `docs/screenshots/html-parity-v293/pause.png`
- `docs/screenshots/html-parity-v293/skills.png`

## 尚未迁移与待确认

- 正式音频素材未提供；AudioManager 仅保留替换接口。
- 正式抖音排行、广告、云存档、支付和商业化不属于旧版闭环，未新增。
- 抖音开发者工具最终预览与真机安全区、触控、性能仍需用户验收。
- 所有视觉和手感结论：**等待用户试玩确认。**
