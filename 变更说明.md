# 《蹦蹦云》v2.12.0 变更说明

## 版本定位

本版本是 `agent/v2.12.0-pixel-parity` 分支的**像素级复刻版**。依据用户指令"连页面全部一模一样"：不再目测还原，而是用 Playwright 从 HTML 原版逐元素量取真实 DOM 包围盒（getBoundingClientRect，390×844 视口），按 1080/390=2.7692 换算成设计坐标，全界面按实测数据重建布局。测量数据存档于 `tools/browser-regression/artifacts/html-geometry.json`，测量脚本 `measure-html.js`。

## 复刻方式

- 每个元素的中心坐标、宽高、字号全部来自 DOM 实测值 × 2.7692，不再手工估算。
- 主页：金币徽章 (-365,1058) 240×133 胶囊（💰 图标与数字分离）、齿轮 (424,1058) 122 圆、logo 区 (0,774)（云 907/标题 730/副标题 627/提示 568）、开始按钮 (0,363) 969×166 圆角 44、卡片 471×327 @ x±249 行 50/-305/-659、版本 (0,-911)、提示 (0,-979)。
- HUD：圆钮 133×133（主页 -440/暂停 -285）、分数中心距顶 85、金币 = 💰 emoji + 金色数字（HTML #coinDisplay 同构）、技能栏 133 与圆钮同排。
- 暂停面板：886×1196、标题 72 @472、继续胶囊 753×152 @292、菜单钮 665×130 @118/-68/-253、主页粉钮 753×125 @-458——**与 HTML 逐值一致**。
- 设置面板 918×1000；皮肤面板 972×1210（格 264×380）；技能商店 972×1210（卡 848×185、图标底 122）；排行榜 752×1180（行 628×111）；统计 752×1000。游戏侧（暂停菜单打开的）面板全部同步。
- 结算面板：886×1250、分数 133px、新纪录金色、三按钮纵排 753 宽（HTML gameOverOverlay 同构）。
- 关卡完成面板：886×1250、🎉 164、标题 93。

## 与 HTML 的有意的差异（功能保留）

- 暂停面板去掉 Cocos 自加的"重新开始"按钮（HTML 原版无此按钮）；重开路径 = 结算面板"再来一次"。
- 设置面板深色模式为真开关（HTML 原版是静态文字"始终开启"，Cocos 版功能更强）。
- 保留 HTML 没有的：教学面板、HUD 统计条、动态状态行（均按 HTML 面板风格造型）。

## 物理极限（无法 100% 相同的三点）

1. CSS backdrop-blur 毛玻璃 → 半透明深色近似（观感约 95%）。
2. 浏览器与 Cocos 的字体渲染引擎微差（字重/抗锯齿）。
3. emoji 字形跟随系统（两边同用系统 emoji）。

## 验证结果（2026-09-14）

- 构建：web-mobile 构建成功；TypeScript 业务代码 0 报错。
- 真实 Chrome 回归 **10/10 全通过**（v2.12.0 标签、移动 199→119→324、暂停/恢复、设置 Toggle 持久化、音频播放、控制台零错误）。
- 截图与 HTML 原版逐屏目视对比：主页、暂停面板布局比例一致。

## 待用户试玩确认

逐屏对照 HTML 原版检查是否达到"一模一样"的预期；进入游戏的手感与动画节奏。
